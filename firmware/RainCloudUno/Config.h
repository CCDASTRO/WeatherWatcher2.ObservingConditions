#pragma once
// Dry-out is configured and enforced by WeatherWatcher on Windows.
constexpr unsigned long SAMPLE_MS = 2000;
constexpr unsigned long STARTUP_MS = 30000;
constexpr unsigned long CONTACT_SETTLE_MS = 100;
